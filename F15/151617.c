#include <stdio.h>
//#include "Boolean.h"
//#include "Linkedlist.h"

int main(){
    char inivarivable[99];
    char iniruangan[99];
    int no, total;

    //void Antrian()
    /*if daftarcheckup(){}*/
        printf("\nStatus Antrian Anda:\n");
        printf("Dokter: %s\n", iniruangan);
        printf("Ruangan: %s\n", iniruangan);
        printf("Posisi Antrian: %d dari %d\n", no, total);
    /*else{}*/
        printf("\nAnda belum terdaftar dalam antrian check-up!\n");
        printf("Silakan daftar terlebih dahulu dengan command DAFTAR_CHECKUP.\n");
}
/*F15
    kondisi login pasien
    Jika Antrian true(dipilih)->
    print(Status antrian Anda:)
    print(Dokter: nama dokter // ini adalah variable)
    print(Ruangan: nama ruangan // ini adalah variable)
    print(Posisi antrian: no antrian // ini adalah variable dari total antrian //ini adalah variable)

    if(belum buat antrian(antrian kosong)) then
        
        print(Anda belum terdaftar dalam antrian check-up!)
        print(Silakan daftar terlebih dahulu dengan command DAFTAR_CHECKUP.)`

*/
/*F16
    for(no obat)->
    if ngobatin true(sudah ada list obat)->
    print(============ DAFTAR OBAT ============)
    print(1. Paracetamol)
    print(2. Amoxicillin)
    print(3. Panadol)

    scanf("pilih obat untuk diminum: %d", &obat) // sesuai no daftar obat
    if(obat in daftar obat){
        printf("GLEKGLEKGLEK... %c berhasil diminum!!!", *list_obat)}
    else{
    printf("Pilihan nomor tidak tersedia!")}
*/
